{
  "name": "frgmt",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dev.mjs",
    "build": "bun build ./index.html ./work.html --outdir=./dist --minify --sourcemap=linked",
    "fetch": "bun run scripts/fetch-repos.mjs",
    "start": "bun run dev"
  }
}
