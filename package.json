{
  "name": "frgmt",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun ./index.html",
    "build": "bun build ./index.html --outdir=./dist --minify --sourcemap=linked",
    "start": "bun run dev"
  }
}
